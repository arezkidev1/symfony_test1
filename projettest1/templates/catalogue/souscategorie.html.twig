{% extends 'base.html.twig' %}

{% block title %}Hello souscatégorie{% endblock %}

{% block body %}

 <div class="container">
        <div class="row">
                {% for categorie in categories %}
                {% if (categorie.produits)|length != null %}
                    <div class="col-4">
                    <a href="{{path('app_produit', {'categorie' : categorie.id }) }}">
                        <img class="img-fluid" src="https://picsum.photos/300/200" >
                        <div class="text-center">
                        {{ categorie.name}}
                        </div>
                    </a>
                    </div>
                    {% endif %}
                {% endfor %}
        </div>
    </div>
{% endblock %}
